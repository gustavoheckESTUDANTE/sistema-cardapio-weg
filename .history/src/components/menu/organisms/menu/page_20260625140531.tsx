import { Product } from "@/types/product";

export default async function Menu() {
    const meatList : Array<Product> = new Array;
    const entryList : Array<Product>
    const principalList : Array<Product>
    const desserts  : Array<Product>
}